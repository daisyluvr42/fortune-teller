# 分析维度与 Prompt 匹配审查

## 结论
当前“分析维度”与后端 `ANALYSIS_PROMPTS` 的键不完全吻合，会导致部分维度落入默认提示。

## 前端维度（`web/app/analysis/page.tsx`）
- 整体命格
- 事业运势
- 财运分析
- 婚恋感情
- 健康体魄

## 后端 Prompt 键（`backend/logic.py` → `ANALYSIS_PROMPTS`）
- 整体命格
- 事业运势
- 感情运势
- 健康建议
- 开运建议
- 大运流年
- 合盘分析

## 不匹配项（会落入默认提示）
- `财运分析`（前端）→ 后端无对应键
- `婚恋感情`（前端）→ 后端对应键为 `感情运势`
- `健康体魄`（前端）→ 后端对应键为 `健康建议`

## 建议修复方案
- 方案 A（推荐）：在后端 `ANALYSIS_PROMPTS` 增加别名键
  - `财运分析` → 新增 prompt 或映射至合适 prompt
  - `婚恋感情` → 映射到 `感情运势`
  - `健康体魄` → 映射到 `健康建议`
- 方案 B：前端改 ID 与后端键一致

## 影响
上述不匹配项会触发默认提示 `"请进行综合命理分析。"`，与标签含义不一致。
